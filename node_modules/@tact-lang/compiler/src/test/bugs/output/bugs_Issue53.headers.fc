;;
;; Header files for Issue53
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_verify_address
slice __tact_verify_address(slice address) inline;

;; $Issue53$_contract_init
tuple $Issue53$_contract_init() impure inline_ref;

;; $Issue53$_contract_load
tuple $Issue53$_contract_load() impure inline_ref;

;; $Issue53$_contract_store
() $Issue53$_contract_store(tuple v) impure inline;
