#pragma version =0.4.4;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

#include "address_AddressTester.headers.fc";
#include "address_AddressTester.stdlib.fc";
#include "address_AddressTester.constants.fc";
#include "address_AddressTester.storage.fc";

;;
;; Contract AddressTester functions
;;

tuple $AddressTester$_contract_init() impure inline_ref {
    tuple $self = null();
    return $self;
}

(tuple, slice) $AddressTester$_fun_test1(tuple $self) impure inline_ref {
    var ($self) = $self;
    return ($self, __gen_slice_address_91f149c50d8445a06cffd875e02ab7777b172ad62464c2492d41da881eddab43());
}

(tuple, slice) $AddressTester$_fun_test2(tuple $self) impure inline_ref {
    var ($self) = $self;
    return ($self, __gen_slice_address_91f149c50d8445a06cffd875e02ab7777b172ad62464c2492d41da881eddab43());
}

(tuple, slice) $AddressTester$_fun_test3(tuple $self) impure inline_ref {
    var ($self) = $self;
    return ($self, __gen_slice_address_91f149c50d8445a06cffd875e02ab7777b172ad62464c2492d41da881eddab43());
}

;;
;; Receivers of a Contract AddressTester
;;

((tuple), ()) %$AddressTester$_internal_empty(tuple $self) impure inline {
    var $self = $self;
    return ($self, ());
}

;;
;; Get methods of a Contract AddressTester
;;

_ %test1() method_id(70304) {
    var self = $AddressTester$_contract_load();
    var res = self~$AddressTester$_fun_test1();
    return res;
}

_ %test2() method_id(74435) {
    var self = $AddressTester$_contract_load();
    var res = self~$AddressTester$_fun_test2();
    return res;
}

_ %test3() method_id(78562) {
    var self = $AddressTester$_contract_load();
    var res = self~$AddressTester$_fun_test3();
    return res;
}

_ supported_interfaces() method_id {
    return (
        "org.ton.introspection.v0"H >> 128,
        "org.ton.abi.ipfs.v0"H >> 128,
        "org.ton.deploy.lazy.v0"H >> 128,
        "org.ton.debug.v0"H >> 128,
        "org.ton.chain.workchain.v0"H >> 128
    );
}

_ get_abi_ipfs() method_id {
    return "ipfs://QmSyRkdnZfTsj1EY14zz4FxVEmThUePjvXpWLvMzEzhBp8";
}

_ lazy_deployment_completed() method_id {
    return get_data().begin_parse().load_int(1);
}

;;
;; Routing of a Contract AddressTester
;;

(tuple, int) $AddressTester$_contract_router_internal(tuple self, int msg_bounced, slice in_msg) impure inline_ref {
    ;; Handle bounced messages
    if (msg_bounced) {
        return (self, true);
    }
    
    ;; Parse incoming message
    int op = 0;
    if (slice_bits(in_msg) >= 32) {
        op = in_msg.preload_uint(32);
    }
    
    
    ;; Receive empty message
    if ((op == 0) & (slice_bits(in_msg) <= 32)) {
        self~%$AddressTester$_internal_empty();
        return (self, true);
    }
    
    return (self, false);
}

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    var msg_flags = cs~load_uint(4);
    var msg_bounced = -(msg_flags & 1);
    slice msg_sender_addr = __tact_verify_address(cs~load_msg_addr());
    __tact_context = (msg_bounced, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var self = $AddressTester$_contract_load();
    
    ;; Handle operation
    int handled = self~$AddressTester$_contract_router_internal(msg_bounced, in_msg);
    
    ;; Throw if not handled
    throw_unless(130, handled);
    
    ;; Persist state
    $AddressTester$_contract_store(self);
}
